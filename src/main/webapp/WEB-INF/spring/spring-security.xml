<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:security="http://www.springframework.org/schema/security"
	xsi:schemaLocation="http://www.springframework.org/schema/security http://www.springframework.org/schema/security/spring-security.xsd http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd">

	<security:http>
		<security:intercept-url pattern="/" access="permitAll" />
		<security:intercept-url pattern="/mypage" access="hasAuthority('USER')" />
		<security:intercept-url pattern="/board/write" access="hasAuthority('USER')" />
		<security:intercept-url pattern="/board/delete" access="hasAuthority('USER')" />
		<security:intercept-url pattern="/board/update" access="hasAuthority('USER')" />
		<security:intercept-url pattern="/board/update*" access="hasAuthority('USER')" />
		<security:intercept-url pattern="/comment/new" access="hasAuthority('USER')" />
		
		<security:form-login login-page="/login"
							 username-parameter="email"
							 login-processing-url="/login.do"
							 default-target-url="/" />
		<!--　最大1個のセッションだけ生成できるようにします。 -->
		<security:session-management invalid-session-url="/login">
			<security:concurrency-control max-sessions="1"
										  expired-url="/login"
										  error-if-maximum-exceeded="true" />
		</security:session-management>
	</security:http>
	
	<security:authentication-manager>
		<security:authentication-provider>
			<security:password-encoder hash="bcrypt"/>
			<security:jdbc-user-service data-source-ref="dataSource"
				users-by-username-query="SELECT email, password, is_active FROM users WHERE email = ?"
				authorities-by-username-query="SELECT email, role as authority FROM users WHERE email = ?"
			/>
		</security:authentication-provider>
	</security:authentication-manager>
	
	<bean id="passwordEncoder" class="org.springframework.security.crypto.bcrypt.BCryptPasswordEncoder" />

</beans>
